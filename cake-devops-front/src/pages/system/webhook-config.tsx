import React from "react";
import styles from "./webhook-config.less";

export default function Page() {
  return (
    <div>
      <h1 className={styles.title}>Page webhook-config</h1>
    </div>
  );
}
