import React from 'react';
import styles from './host-group-list.less';

export default function Page() {
  return (
    <div>
      <h1 className={styles.title}>Page host-group-list</h1>
    </div>
  );
}
