import React from 'react';
import styles from './server-proxy.less';

export default function Page() {
  return (
    <div>
      <h1 className={styles.title}>Page server-proxy</h1>
    </div>
  );
}
